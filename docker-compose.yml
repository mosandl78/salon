version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_USER: salon
      POSTGRES_PASSWORD: salon
      POSTGRES_DB: salon_dev
    ports:
      - "5433:5432"
    volumes:
      - salon_pgdata:/var/lib/postgresql/data
volumes:
  salon_pgdata:
